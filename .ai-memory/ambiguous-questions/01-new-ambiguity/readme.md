# Open Ambiguities

Currently, there are **0 open ambiguities**.
When an underspecified requirement arises, draft `01-<slug>.md` here.
Upon user clarification, move (`mv`) to `../02-ambiguity-resolved/01-<slug>.md` and append `## Resolution`.
