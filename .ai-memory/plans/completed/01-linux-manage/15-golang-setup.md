# Task 15: Translate Go (Golang) Setup
## Where
- `01-ubuntu-installation.md`, `02-fedora-installation.md`, `03-centos-installation.md`
## What
Translate: `wget https://go.dev/dl/go1.22.3.linux-amd64.tar.gz`, `tar -C /usr/local -xzf`, `export PATH=$PATH:/usr/local/go/bin`.
## How
- **All**: Provide exact download commands, `sudo rm -rf /usr/local/go`, `sudo tar -C /usr/local -xzf ...`, and update `.zshrc` / `.bashrc` path exports.
