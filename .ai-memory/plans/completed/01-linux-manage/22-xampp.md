# Task 22: Translate XAMPP Download & Setup
## Where
- `01-ubuntu-installation.md`, `02-fedora-installation.md`, `03-centos-installation.md`
## What
Translate: `xampp-linux-x64-8.2.12-0-installer.run` execute and install.
## How
- **All**: Download using `aria2c` or `wget`. Run `chmod +x` on the `.run` file and execute with `sudo`. Same across all distributions.
