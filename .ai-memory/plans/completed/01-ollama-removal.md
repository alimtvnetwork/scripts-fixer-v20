# Subtask 1: Ollama Removal

## Actions
- Edit `scripts/os/ubuntu/profile-ubuntu-dev-ai.sh` to remove `install-models.sh` or any other Ollama-related scripts.
- Check Windows profiles (e.g. `scripts/os/windows/profile-windows-dev-ai.ps1` or similar) to ensure Ollama is not included in AI profiles.
- Ensure the user's request "WHY olma with ubuntu-ai, why?... don't add OLMA please" is satisfied across all OS AI profiles.
