# Plan: Profile Trees and Ollama Removal

## Objectives
1. Remove Ollama from AI profiles.
2. Add Profile Tree View & Examples to `run.sh` and `run.ps1`.
3. Release Bump to v1.37.0.

## Subtasks
1. `01-ollama-removal.md`: Remove `install-models.sh` or Ollama related scripts from AI profiles (Ubuntu and Windows).
2. `02-tree-view.md`: Document and implement/verify profile tree view (`tree` and `--dry-run`) in `run.ps1` and `run.sh`. Add explicit examples.
3. `03-release.md`: Bump release version to v1.37.0.
