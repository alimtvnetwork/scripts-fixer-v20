# Task 17: Translate MiniKube
## Where
- `01-ubuntu-installation.md`, `02-fedora-installation.md`, `03-centos-installation.md`
## What
Translate: `aria2c ... minikube-linux-amd64`, `sudo install minikube-linux-amd64 /usr/local/bin/minikube`.
## How
- **All**: Provide the download command using `wget` or `aria2c`. Show installation via `sudo install`. Include startup command `minikube start`.
