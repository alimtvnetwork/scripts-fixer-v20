# Task 29: Translate PowerShell for Linux
## Where
- `01-ubuntu-installation.md`, `02-fedora-installation.md`, `03-centos-installation.md`
## What
Translate: `apt-fast install powershell`, `packages-microsoft-prod.deb`.
## How
- **Ubuntu**: Install using the `.deb` repo from Microsoft.
- **Fedora/CentOS**: Install using the `.rpm` repo from Microsoft (`rpm --import`, then `dnf install powershell`).
