# Subtask 3: Release v1.36.0

## Scope
Finalize changes, update versions, and prepare for release.

## Steps
1. **Version Bumps**:
   - Update version string in `run.ps1` to `v1.36.0`.
   - Update version string in `run.sh` to `v1.36.0`.

2. **Changelog**:
   - Add entry for v1.36.0 detailing:
     - Added `qtorrent` and `utorrent` installers.
     - Added `export-config` and `import-config` features for `qtorrent`, `utorrent`, and `vscode`.

3. **Validation**:
   - Ensure all scripts pass basic syntax checks.
   - Review help output manually (dry run).
