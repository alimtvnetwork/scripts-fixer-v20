# Task 30: Translate User Management & Final Commits
## Where
- `01-ubuntu-installation.md`, `02-fedora-installation.md`, `03-centos-installation.md`
## What
Translate: `useradd -m -d "$homedir" -s /bin/bash "$username"`, `crontab -l`, `@reboot /home/newuser/first_login.sh`.
## How
- **All**: Ensure user management commands are consistent across all three markdowns, as they are mostly POSIX standard.
- Instruct execution agent to format files, check limits, and group all completed markdown work into a single logical commit and push.
