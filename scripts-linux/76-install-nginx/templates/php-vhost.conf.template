# Auto-generated by scripts-fixer Nginx Domain Manager (Type: php)
server {
    listen {{PORT}};
    listen [::]:{{PORT}};
    server_name {{DOMAIN}};
    root {{ROOT}};
    index index.php index.html index.htm;

    include /etc/nginx/snippets/scripts-fixer-security.conf;
    include /etc/nginx/snippets/scripts-fixer-static.conf;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass {{PHP_SOCKET}};
        include /etc/nginx/snippets/scripts-fixer-fastcgi.conf;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }
    location ~ /\. { deny all; access_log off; log_not_found off; }
}
