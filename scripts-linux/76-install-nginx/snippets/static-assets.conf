# scripts-fixer static asset caching and compression optimization
location ~* \.(jpg|jpeg|gif|png|webp|svg|ico|css|js|woff|woff2|ttf|eot|otf|mp4|webm|ogv)$ {
    expires 30d;
    add_header Cache-Control "public, no-transform, max-age=2592000";
    access_log off;
    log_not_found off;
}
